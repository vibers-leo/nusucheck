<div class="space-y-6 py-8">
  <!-- 헤더 -->
  <div class="text-center mb-8">
    <h1 class="text-3xl font-bold text-gray-900">💼 작성한 보험청구서</h1>
    <p class="mt-2 text-gray-600">고객을 위해 작성한 보험청구서를 관리하세요</p>
  </div>

  <!-- 통계 카드 -->
  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    <div class="bg-gradient-to-br from-primary-50 to-indigo-50 rounded-xl p-5 border border-primary-100">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-sm text-primary-700 font-medium">작성 중</p>
          <p class="text-3xl font-bold text-primary-900 mt-1"><%= @insurance_claims.by_status(:draft).count %></p>
        </div>
        <span class="text-4xl">✏️</span>
      </div>
    </div>
    <div class="bg-gradient-to-br from-yellow-50 to-amber-50 rounded-xl p-5 border border-yellow-100">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-sm text-yellow-700 font-medium">고객 확인 대기</p>
          <p class="text-3xl font-bold text-yellow-900 mt-1"><%= @insurance_claims.by_status(:pending_customer_review).count %></p>
        </div>
        <span class="text-4xl">⏱️</span>
      </div>
    </div>
    <div class="bg-gradient-to-br from-green-50 to-emerald-50 rounded-xl p-5 border border-green-100">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-sm text-green-700 font-medium">제출 완료</p>
          <p class="text-3xl font-bold text-green-900 mt-1"><%= @insurance_claims.where(status: [:submitted, :under_review, :approved, :completed]).count %></p>
        </div>
        <span class="text-4xl">✅</span>
      </div>
    </div>
  </div>

  <!-- 카드형 목록 -->
  <% if @insurance_claims.any? %>
    <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      <% @insurance_claims.each do |claim| %>
        <%= link_to masters_insurance_claim_path(claim), class: "block bg-white rounded-2xl border border-gray-100 hover:shadow-md transition" do %>
          <div class="p-5">
            <div class="flex items-center justify-between mb-3">
              <span class="text-sm font-mono text-gray-500"><%= claim.claim_number %></span>
              <%= render "shared/status_badge", status: claim.status %>
            </div>

            <h3 class="font-semibold text-gray-900 mb-1">
              <%= claim.customer&.name || "고객" %>
            </h3>

            <p class="text-sm text-gray-600 mb-2"><%= claim.applicant_name %></p>

            <div class="space-y-1 text-sm text-gray-600">
              <p class="flex items-center">
                <svg class="w-4 h-4 mr-1.5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                </svg>
                <%= truncate(claim.incident_address, length: 30) %>
              </p>
              <p class="flex items-center">
                <svg class="w-4 h-4 mr-1.5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                </svg>
                사고일: <%= claim.incident_date.strftime("%Y-%m-%d") %>
              </p>
              <% if claim.estimated_damage_amount.present? %>
                <p class="flex items-center text-primary-600 font-medium">
                  <svg class="w-4 h-4 mr-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"/>
                  </svg>
                  <%= number_to_currency(claim.estimated_damage_amount, unit: "₩", precision: 0) %>
                </p>
              <% end %>
            </div>

            <% if claim.pending_customer_review? %>
              <div class="mt-3 pt-3 border-t border-gray-100">
                <p class="text-xs text-yellow-700 bg-yellow-50 rounded-lg px-2 py-1">
                  ⏱️ 고객 확인 대기 중
                </p>
              </div>
            <% end %>
          </div>
        <% end %>
      <% end %>
    </div>

    <!-- 페이지네이션 -->
    <div class="mt-6">
      <%= paginate @insurance_claims %>
    </div>
  <% else %>
    <%= render "shared/empty_state",
          icon: "document",
          title: "아직 작성한 보험청구서가 없어요",
          description: "고객을 위해 보험청구서를 작성해보세요" %>

    <!-- 안내 카드 -->
    <div class="mt-8 bg-gradient-to-br from-primary-50 to-indigo-50 rounded-2xl p-6 border border-primary-100">
      <div class="flex items-center gap-2 mb-4">
        <span class="text-2xl">💡</span>
        <h3 class="font-bold text-gray-900">보험청구서는 언제 작성하나요?</h3>
      </div>
      <div class="space-y-3 text-sm text-gray-700">
        <p>공사를 완료하고 고객이 보험청구를 원할 때 <strong>대신 작성</strong>해드릴 수 있어요</p>
        <div class="grid grid-cols-1 md:grid-cols-4 gap-3 mt-4 pt-4 border-t border-primary-200">
          <div class="bg-white rounded-lg p-3">
            <p class="font-semibold text-gray-900 mb-1 text-xs">1️⃣ 신고 확인</p>
            <p class="text-gray-600 text-xs">내 작업 목록에서<br/>완료된 신고 선택</p>
          </div>
          <div class="bg-white rounded-lg p-3">
            <p class="font-semibold text-gray-900 mb-1 text-xs">2️⃣ 청구서 작성</p>
            <p class="text-gray-600 text-xs">견적서, 영수증<br/>사진 첨부</p>
          </div>
          <div class="bg-white rounded-lg p-3">
            <p class="font-semibold text-gray-900 mb-1 text-xs">3️⃣ 고객 검토</p>
            <p class="text-gray-600 text-xs">고객이 내용 확인<br/>후 승인</p>
          </div>
          <div class="bg-white rounded-lg p-3">
            <p class="font-semibold text-gray-900 mb-1 text-xs">4️⃣ 자동 제출</p>
            <p class="text-gray-600 text-xs">승인되면 보험사에<br/>자동 접수</p>
          </div>
        </div>
      </div>
    </div>

    <!-- 장점 안내 -->
    <div class="mt-6 bg-gradient-to-br from-green-50 to-emerald-50 rounded-2xl p-6 border border-green-100">
      <div class="flex items-center gap-2 mb-4">
        <span class="text-2xl">🌟</span>
        <h3 class="font-bold text-gray-900">고객 대신 작성하면 좋은 점</h3>
      </div>
      <ul class="space-y-2 text-sm text-gray-700">
        <li class="flex items-start gap-2">
          <span class="flex-shrink-0 w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs font-bold">✓</span>
          <span><strong>고객 만족도 향상</strong> - 보험청구까지 도와드려 신뢰도 UP</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="flex-shrink-0 w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs font-bold">✓</span>
          <span><strong>정확한 서류 작성</strong> - 전문가가 작성해 반려율 DOWN</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="flex-shrink-0 w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs font-bold">✓</span>
          <span><strong>추가 수입 가능</strong> - 서류 작성 대행비를 받을 수 있어요</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="flex-shrink-0 w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs font-bold">✓</span>
          <span><strong>고객 편의성</strong> - 복잡한 서류를 대신 작성해드려요</span>
        </li>
      </ul>
    </div>

    <!-- CTA -->
    <div class="mt-6 text-center">
      <%= link_to masters_requests_path, class: "inline-flex items-center gap-2 px-8 py-4 bg-primary-600 text-white rounded-xl font-bold hover:bg-primary-700 transition shadow-lg hover:shadow-xl" do %>
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
        </svg>
        내 작업 목록에서 시작하기
      <% end %>
    </div>
  <% end %>
</div>
