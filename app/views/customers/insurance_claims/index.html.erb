<div class="max-w-4xl mx-auto py-8">
  <div class="text-center mb-8">
    <h1 class="text-3xl font-bold text-gray-900">💼 내 보험청구 내역</h1>
    <p class="mt-2 text-gray-600">누수로 피해를 입으셨나요? 일상배상책임보험으로 보상받으세요</p>
  </div>

  <% if @insurance_claims.any? %>
    <div class="space-y-4">
      <% @insurance_claims.each do |claim| %>
        <%= link_to customers_insurance_claim_path(claim),
              class: "block bg-white rounded-2xl border border-gray-100 hover:shadow-md transition p-5" do %>
          <div class="flex justify-between items-start">
            <div class="flex-1">
              <div class="flex items-center gap-2">
                <span class="text-sm font-mono text-gray-500"><%= claim.claim_number %></span>
                <span class="px-3 py-1 rounded-full text-xs font-medium
                  <%= case claim.status
                      when 'draft' then 'bg-gray-100 text-gray-700'
                      when 'submitted' then 'bg-primary-100 text-primary-700'
                      when 'under_review' then 'bg-yellow-100 text-yellow-700'
                      when 'approved' then 'bg-green-100 text-green-700'
                      when 'rejected' then 'bg-red-100 text-red-700'
                      when 'completed' then 'bg-purple-100 text-purple-700'
                      end %>" role="status">
                  <%= claim.status_label %>
                </span>
              </div>
              <h3 class="font-semibold text-gray-900 mt-2">
                📍 <%= claim.incident_address.truncate(40) %>
              </h3>
              <div class="flex flex-wrap gap-3 mt-2 text-sm text-gray-600">
                <span>🗓️ <%= claim.incident_date.strftime("%Y년 %m월 %d일") %></span>
                <% if claim.estimated_damage_amount.present? %>
                  <span>💰 <%= number_to_currency(claim.estimated_damage_amount, unit: "", precision: 0) %>원</span>
                <% end %>
                <% if claim.damage_type.present? %>
                  <span><%= claim.damage_type_label %></span>
                <% end %>
              </div>
            </div>
            <svg class="w-5 h-5 text-gray-400 flex-shrink-0 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
            </svg>
          </div>
        <% end %>
      <% end %>
    </div>

    <div class="mt-8 text-center">
      <%= link_to new_customers_insurance_claim_path,
            class: "inline-flex items-center gap-2 px-6 py-3 bg-primary-600 text-white rounded-xl font-semibold hover:bg-primary-700 transition shadow-lg" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
        </svg>
        새 보험 신청하기
      <% end %>
    </div>

    <div class="mt-6">
      <%= paginate @insurance_claims %>
    </div>
  <% else %>
    <%= render "shared/empty_state",
          icon: "document",
          title: "아직 보험 신청 내역이 없어요",
          description: "누수로 인한 피해를 보상받을 수 있어요" %>

    <!-- 보험청구 안내 -->
    <div class="mt-8 bg-gradient-to-br from-primary-50 to-indigo-50 rounded-2xl p-6 border border-primary-100">
      <div class="flex items-center gap-2 mb-4">
        <span class="text-2xl">💡</span>
        <h3 class="font-bold text-gray-900">이런 경우 보험청구가 가능해요</h3>
      </div>
      <ul class="space-y-3 text-sm text-gray-700">
        <li class="flex items-start gap-3">
          <span class="flex-shrink-0 w-6 h-6 bg-primary-500 text-white rounded-full flex items-center justify-center text-xs font-bold">✓</span>
          <span><strong>내 집 누수로 아래층에 피해</strong>를 입힌 경우</span>
        </li>
        <li class="flex items-start gap-3">
          <span class="flex-shrink-0 w-6 h-6 bg-primary-500 text-white rounded-full flex items-center justify-center text-xs font-bold">✓</span>
          <span><strong>배관 파열로 이웃 집에 손해</strong>를 끼친 경우</span>
        </li>
        <li class="flex items-start gap-3">
          <span class="flex-shrink-0 w-6 h-6 bg-primary-500 text-white rounded-full flex items-center justify-center text-xs font-bold">✓</span>
          <span><strong>수리 비용을 보험으로 처리</strong>하고 싶은 경우</span>
        </li>
      </ul>
      <div class="mt-6 pt-4 border-t border-primary-200">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
          <div>
            <p class="font-semibold text-gray-900 mb-1">📋 필요한 서류</p>
            <p class="text-gray-600">사진, 수리 견적서, 영수증 등</p>
          </div>
          <div>
            <p class="font-semibold text-gray-900 mb-1">⏱️ 처리 기간</p>
            <p class="text-gray-600">평균 7~14일 (보험사마다 다름)</p>
          </div>
        </div>
      </div>
    </div>

    <div class="mt-6 text-center">
      <%= link_to new_customers_insurance_claim_path,
            class: "inline-flex items-center gap-2 px-8 py-4 bg-primary-600 text-white rounded-xl font-bold text-lg hover:bg-primary-700 transition shadow-lg hover:shadow-xl" do %>
        💼 보험 신청하기
      <% end %>
      <p class="text-xs text-gray-500 mt-3">신청서 작성은 5분이면 충분해요</p>
    </div>
  <% end %>
</div>
