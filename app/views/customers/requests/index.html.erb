<%# 고객 - 내 체크 목록 (역할 맞춤 재설계) %>

<%
  current_status = params.dig(:q, :status_eq).to_s
  current_symptom = params.dig(:q, :symptom_type_eq).to_s
  has_filter = current_status.present? || current_symptom.present?

  status_label = {
    "reported"                => "접수됨",
    "assigned"                => "전문가 배정",
    "visiting"                => "방문 중",
    "detecting"               => "탐지 중",
    "no_leak_found"           => "누수 미확인",
    "estimate_pending"        => "견적 대기",
    "estimate_submitted"      => "견적 확인",
    "construction_agreed"     => "공사 합의",
    "escrow_deposited"        => "결제 완료",
    "constructing"            => "공사 중",
    "construction_completed"  => "완료 확인",
    "closed"                  => "종료",
    "cancelled"               => "취소"
  }

  next_action = ->(req) {
    case req.status
    when "reported"               then "전문가 매칭 중이에요"
    when "assigned"               then "전문가가 일정을 잡고 있어요"
    when "visiting"               then "전문가 방문 예정이에요"
    when "detecting"              then "누수 탐지가 진행 중이에요"
    when "no_leak_found"          then "탐지 결과를 확인해주세요"
    when "estimate_pending"       then "견적을 기다리는 중이에요"
    when "estimate_submitted"     then "견적이 도착했어요 → 확인해주세요"
    when "construction_agreed"    then "공사 일정이 확정됐어요"
    when "escrow_deposited"       then "결제 완료 · 공사 시작 예정이에요"
    when "constructing"           then "공사가 진행 중이에요"
    when "construction_completed" then "공사가 끝났어요 → 완료 확인해주세요"
    else nil
    end
  }
%>

<!-- 헤더 -->
<div class="mb-5 flex items-center justify-between">
  <div>
    <h1 class="text-xl font-bold text-gray-900">내 체크</h1>
    <p class="text-sm text-gray-500 mt-0.5">의뢰하신 누수 체크 현황이에요</p>
  </div>
  <%= link_to new_customers_request_path,
        class: "flex items-center gap-1.5 px-4 py-2 bg-primary-600 text-white text-sm font-bold rounded-xl hover:bg-primary-700 transition-colors shadow-sm" do %>
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
    </svg>
    새 체크
  <% end %>
</div>

<!-- ── 필터 버튼 (드롭다운 없이 나열) ── -->
<div class="space-y-3 mb-5">

  <!-- 상태 필터 -->
  <div>
    <p class="text-xs font-semibold text-gray-400 mb-2">상태</p>
    <div class="flex gap-2 flex-wrap">
      <% [
        ["전체", ""],
        ["접수됨", "reported"],
        ["진행중", "visiting"],
        ["탐지중", "detecting"],
        ["견적 확인", "estimate_submitted"],
        ["공사중", "constructing"],
        ["완료 확인", "construction_completed"],
        ["종료", "closed"],
        ["취소", "cancelled"]
      ].each do |label, value| %>
        <%
          is_active = current_status == value || (value.empty? && current_status.empty?)
          href = customers_requests_path(q: { status_eq: value.presence, symptom_type_eq: current_symptom.presence }.compact)
        %>
        <%= link_to label, href,
              class: "flex-none px-3.5 py-1.5 rounded-full text-xs font-semibold transition-colors #{
                is_active ? 'bg-primary-500 text-white shadow-sm' : 'bg-white border border-gray-200 text-gray-600 hover:border-primary-300 hover:text-primary-600'
              }" %>
      <% end %>
    </div>
  </div>

  <!-- 증상 필터 -->
  <div>
    <p class="text-xs font-semibold text-gray-400 mb-2">증상</p>
    <div class="flex gap-2 flex-wrap">
      <% [
        ["전체", ""],
        ["벽면 💧", "wall_leak"],
        ["천장 💧", "ceiling_leak"],
        ["바닥 💧", "floor_leak"],
        ["배관 🔧", "pipe_leak"],
        ["화장실 🚽", "toilet_leak"],
        ["외부 🏠", "outdoor_leak"]
      ].each do |label, value| %>
        <%
          is_active = current_symptom == value || (value.empty? && current_symptom.empty?)
          href = customers_requests_path(q: { status_eq: current_status.presence, symptom_type_eq: value.presence }.compact)
        %>
        <%= link_to label, href,
              class: "flex-none px-3.5 py-1.5 rounded-full text-xs font-semibold transition-colors #{
                is_active ? 'bg-primary-500 text-white shadow-sm' : 'bg-white border border-gray-200 text-gray-600 hover:border-primary-300 hover:text-primary-600'
              }" %>
      <% end %>
    </div>
  </div>
</div>

<!-- ── 리스트 ── -->
<% if @requests.any? %>
  <div class="bg-white rounded-2xl border border-gray-100 divide-y divide-gray-50">
    <% @requests.each do |request| %>
      <%= link_to customers_request_path(request), class: "block hover:bg-gray-50 transition-colors" do %>
        <div class="px-4 py-4">
          <div class="flex items-start justify-between">
            <div class="flex-1 min-w-0">
              <div class="flex items-center gap-2 mb-1.5">
                <%= render "shared/status_badge", status: request.status %>
                <span class="text-xs text-gray-400">#<%= request.id %></span>
              </div>

              <h3 class="text-[15px] font-semibold text-gray-900 mb-1">
                <%= symptom_label(request.symptom_type) %>
              </h3>

              <div class="flex items-center gap-2 text-xs text-gray-400">
                <svg class="w-3 h-3 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0zM15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                </svg>
                <span class="truncate"><%= truncate(request.address, length: 24) %></span>
                <span class="text-gray-200 flex-shrink-0">·</span>
                <span class="flex-shrink-0"><%= request.created_at.strftime("%m.%d") %></span>
              </div>

              <% hint = next_action.call(request) %>
              <% if hint && !%w[closed cancelled].include?(request.status) %>
                <p class="mt-1.5 text-xs text-primary-500 font-medium"><%= hint %></p>
              <% end %>
            </div>

            <svg class="w-4 h-4 text-gray-300 flex-shrink-0 ml-3 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
            </svg>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>

  <div class="mt-6">
    <%= paginate @requests %>
  </div>

<% else %>
  <%= render "shared/empty_state",
        icon: "water",
        title: has_filter ? "해당 조건의 체크가 없어요" : "아직 체크 내역이 없어요",
        description: has_filter ? "다른 필터를 선택해보세요" : "누수로 고민이시라면 지금 바로 체크하세요",
        cta_text: has_filter ? "전체 보기" : "체크 접수하기",
        cta_path: has_filter ? customers_requests_path : new_customers_request_path %>

  <% unless has_filter %>
    <div class="mt-6 bg-primary-50 rounded-2xl p-5">
      <h3 class="text-sm font-bold text-gray-900 mb-3">이런 증상이 있다면 바로 체크하세요</h3>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 text-sm text-gray-700">
        <% [
          "벽이나 천장에 물방울이 맺혀있어요",
          "벽지가 들뜨거나 변색되었어요",
          "곰팡이 냄새가 나요",
          "수도 요금이 갑자기 많이 나왔어요"
        ].each do |item| %>
          <div class="flex items-center gap-2">
            <div class="w-5 h-5 bg-primary-500 text-white rounded-full flex items-center justify-center text-[10px] font-bold flex-shrink-0">✓</div>
            <span><%= item %></span>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
<% end %>
